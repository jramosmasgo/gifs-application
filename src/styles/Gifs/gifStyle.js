import { styled } from "@mui/system";

export const ContentGif = styled("div")({
  position: "relative",
});

export const ContentGifTitle = styled("div")({
  position: "absolute",
  bottom: 0,
  left: 0,
  width: "100%",
  height: "max-content",
  background: "#000",
  color: "white",
  padding: "5px",
});
